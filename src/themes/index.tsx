import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    common: {
      black: "#000",
      white: "#fff"
    },
    primary: {
      main: "#FFAD10",
      dark: "#ce8b0c"
    },
    secondary: {
      main: "#fff"
    },
    grey: {
      900: "#171717",
    },
  },
});
